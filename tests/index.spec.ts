import 'mocha';
import { expect } from 'chai';
import {ArithmeticableCollection} from '../src/index';
import {Rational} from '../src/index';
import {Arithmeticable} from '../src/index';



describe('prueba hexadecimal', () =>{
  it('primer test', () => {
    let numeros = new abstractgArithmeticableCollection<Rational>([1,2,3,4]);
    expect().to.be.equal("")
  });
    
});